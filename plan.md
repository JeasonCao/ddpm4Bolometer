# DDPM 玻璃体降噪模型 — 评估与分析计划

> 本文档记录模型训练完成后的评估工作计划、实现逻辑及待确认事项。

---

## 总体流程

```
模型训练完成
    → Step 1: 验证模型基本性能
    → Step 2: 生成评估数据集
    → Step 3: 批量推理（降噪）
    → Step 4: 脉冲拟合 + 提取物理量
    → Step 5: 出图分析
    → Step 6: 后续改进空间（待规划）
```

---

## Step 1：验证模型基本性能

**逻辑：** 确认模型收敛正常，没有过拟合或欠拟合，推理结果合理。

**具体操作：**
- 检查 `model_output/loss_curve.png`，train/val 曲线应平滑下降且差距不过大
- 用现有数据跑几个典型样本的推理图（`qa_inference.png` 流程）
- 若 val loss 后期明显上翘，需考虑 early stopping 或重训

**无需额外讨论，训练完直接执行。**

---

## Step 2：生成评估数据集

### 2a. 能谱分辨率评估组

**目标：** 评估不同能量、不同 SNR 下，降噪前后的能谱分辨率变化。

**逻辑：**
- 固定探测器参数（取 `src/pulse/simulator.py` 中各参数分布的中间值），每个指定能量生成 **1 个干净模板波形**
- 对每个模板，叠加 **10000 条随机噪声**（从已有噪声数据库中随机采样），得到 10000 个带噪波形
- 每个事件在存储时计算并保存 SNR flag，画图时按 SNR 分 20 个 bin

**能量点：**

| 能量 (keV) | 来源 |
|-----------|------|
| 583 | ²⁰⁸Tl gamma 线（常见本底） |
| 1461 | ⁴⁰K gamma 线（常见本底） |
| 2528 | ¹³⁰Te 0νββ Q 值（CUORE 核心信号） |
| 2615 | ²⁰⁸Tl gamma 线（高能本底标定峰） |
| 3034 | ¹⁰⁰Mo 0νββ Q 值（CUPID 核心信号） |

**固定探测器参数：** 取 `simulator.py` 中各随机参数分布的中间值（代码实现时从源码读取）。

---

### 2c. 小信号重建效率组

**目标：** 在 SNR 极低（2σ 条件）下，评估降噪前后的拟合收敛率提升。

**逻辑：**
- 先模拟一个 100 keV 的信号，测量其幅值 A
- 将噪声 RMS 设为 A/2（即 2σ 条件，SNR ≈ 6 dB）
- 在此固定噪声水平下生成 **5000 个**带噪事件
- 对比降噪前后的拟合收敛率和幅值重建偏差

**核心结论形式：** "对于 SNR = X dB 的 100 keV 事件，降噪后拟合收敛率从 Y% 提升至 Z%"

> **待确认：** 需要先模拟 100 keV 信号测量幅值后，确认 2σ 对应的实际 SNR 数值是否符合预期。

---

## Step 3：批量推理（降噪）

**逻辑：** 对 2a、2c 的所有带噪波形，用训练好的模型批量推理，得到降噪后波形。

**关键优化：批量推理（Batch Inference）**

现有 `inference.py` 逐条处理，速度约：
- 1-shot：~0.83 秒/条，10000 条 ≈ 2.3 小时
- 10-shot：~7.2 秒/条，10000 条 ≈ 20 小时

需要新写批量推理脚本，GPU 一次处理多个波形（batch_size=64 或更大），预计速度提升 10~50 倍，将 10-shot 总时间压缩到可接受范围内。

**流程：**
1. 先用 1-shot 快速验证评估流程无 bug
2. 确认无误后跑正式 10-shot 批量推理
3. 降噪前和降噪后的波形均保存到 HDF5

---

## Step 4：脉冲拟合 + 物理量提取

**逻辑：** 新写评估脚本，批量调用 `src/basics/fit.py` 中的三指数拟合函数，提取物理量，存入结构化 HDF5 文件（模仿 ROOT TTree 逻辑，每行一个事件）。

**三指数函数形式：** 参见 `src/basics/fit.py`

**每个事件存储的字段：**

| 字段 | 说明 |
|------|------|
| `energy_true` | 注入真实能量（keV） |
| `snr_db` | 计算所得 SNR（dB），画图时按此分 bin |
| `amp_noisy` | 降噪前拟合幅值 |
| `amp_denoised` | 降噪后拟合幅值 |
| `amp_maxbase_noisy` | 降噪前 max-baseline 幅值（备用，同步计算） |
| `amp_maxbase_denoised` | 降噪后 max-baseline 幅值（备用，同步计算） |
| `chi2_noisy` | 降噪前 chi²/ndf |
| `chi2_denoised` | 降噪后 chi²/ndf |
| `fit_converged_noisy` | 降噪前拟合是否收敛（bool） |
| `fit_converged_denoised` | 降噪后拟合是否收敛（bool） |
| `t_rise` | 上升时间常数（s） |
| `t_fall1` | 下降时间常数 1（s） |
| `t_fall2` | 下降时间常数 2（s） |
| `noise_rms` | 噪声总 RMS（mV） |
| `noise_alpha` | 1/f 噪声指数 α |
| `noise_ac_frac` | 50 Hz 谐波分量相对占比 |
| `noise_pt_frac` | 脉管冷机谐波分量相对占比 |
| `noise_white_frac` | 白噪声分量相对占比 |
| `noise_colored_frac` | 彩色噪声（1/f + linear）分量相对占比 |
| `noise_n_resonances` | 机械共振峰数量 |

**拟合收敛判据（用户可配置，默认值待定）：**
- `chi2_max`：chi²/ndf 上限（默认值待定）
- `amp_min`、`amp_max`：幅值合理范围
- `amp_dev_max`：幅值偏离真实值的最大容许百分比（用于效率计算）

**max-baseline 说明：** 虽然主分析用三指数拟合，但同步计算并存入 max-baseline 幅值，方便后续对比两种方法的分辨率差异。

---

## Step 5：出图分析

### 图 1：能谱分辨率 vs SNR

- **横轴：** SNR（dB），分 20 个 bin
- **纵轴：** 能谱 FWHM（keV），对每个 SNR bin 内的幅值分布做高斯拟合得到
- **每个能量点一张子图**（或同一张图不同颜色），5 个能量点
- 降噪前（蓝色）vs 降噪后（红色）

### 图 2：小信号重建效率对比

- **场景：** 100 keV，2σ 噪声条件（SNR ≈ 6 dB）
- **内容：** 降噪前后拟合收敛率 + 幅值偏差分布（两张子图）
- **效率定义：** 拟合收敛 且 幅值偏差 < `amp_dev_max`（可配置）

---

## Step 6：后续改进空间

以下方向框架已预留，待主体分析完成后推进：

### 6a. 低能阈值系统性扫描

**目标：** 确定模型在不同噪声水平下的有效探测能量下限。

**逻辑：**
- 能量范围：10~100 keV，取约 10 个等间隔点
- 对每个能量点，在固定噪声水平下生成 1000 个事件
- 画出拟合收敛率 vs 能量的曲线（降噪前后各一条）
- 50% 收敛率对应的能量即为有效阈值

**核心结论形式：** "模型将有效探测阈值从 X keV 降低至 Y keV"

**可用的三个互补判据：**
1. 拟合收敛率（50% 点）
2. 重建幅值系统偏差（> 5% 则失效）
3. 能谱 FWHM 发散点

### 6b. 专项低能训练

**讨论：** 是否对低能段（< 200 keV）做专项训练或增加低能样本比例？

**建议方向：** 不单独训练低能模型，而是在 `generate_clean_shards.py` 中增加低能样本的生成比例（调整 `E_min`/`E_max`），让现有模型在低能段有更丰富的训练样本，避免高能段性能下降。

### 6c. Flow Matching 对比

**目标：** 用相同评估流程对比 DDPM 和 Flow Matching 两个模型的性能差异，作为论文对比实验。

---

## 待确认事项

- [ ] 确认 100 keV 信号幅值，验证 2σ 条件对应的 SNR 是否符合物理预期
- [ ] 拟合收敛判据的具体数值（chi²/ndf 上限、幅值偏差容许范围）
- [ ] 1-shot vs 10-shot 正式分析的选择（待批量推理速度优化后决定）
- [ ] 低能段（Step 6a）的具体推进时机
